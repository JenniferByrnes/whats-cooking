<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>What's Cooking?</title>
    <link
      href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/output.css" />
<link rel="icon" type="image/x-icon" href="/icons/favicon.ico"/>
  </head>

  </head>
  <body>
    <div class="bg-hero">
      <header class="w-full">
        <h1 class="font-sans text-3xl text-blue-700 font-bold">
          <a href="/">Home</a>
        </h1>
        <nav>
          {{#if loggedIn}}
            <a href="/dashboard">Dashboard</a>
            <button id="logout" class="btn-no-style">Logout</button>
          {{else}}
            <a href="/login">Login</a>
          {{/if}}
        </nav>

      </header>
      <header>
        <h1 class="text-3xl font-mono font-bold text-red-600 text-center">What's Cooking?</h1>
      </header>

      <main>
        {{{body}}}
      </main>
      <footer>
       <h2 class="text-2xl font-mono font-bold text-blue-700 text-center">Thanks for visiting!</h2>
      </footer>
    </div>

    {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
    {{/if}}
    <script src="/javascript/searchbar.js"></script>
  </body>

</html>