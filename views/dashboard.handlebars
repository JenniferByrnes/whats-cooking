<section id="create-recipe" class="hide">

  <form class="new-recipe-form">
    <h2>Create New Recipe</h2>
    <div>
      <label for="recipe-title">Title</label>
      <input type="text" id="recipe-title" name="recipe-title" />
    </div>
    <div>
      <label for="recipe-text">Blog Text</label>
      <textarea name="recipe-text"></textarea>
    </div>
        <div>
      <label for="recipe-ingredients">Blog Text</label>
      <textarea name="recipe-ingredients"></textarea>
    </div>
        <div>
      <label for="recipe-cuisines">Blog Text</label>
      <textarea name="recipe-cuisines"></textarea>
    </div>
        <div>
      <label for="recipe-serving">Blog Text</label>
      <textarea name="recipe-serving"></textarea>
    </div>
    <button type="add" class="btn">Add</button>
  </form>
</section>

<section id="display-recipes">
  <h2>Your Recipes</h2>
  <button id="create-btn" type="submit" class="btn">Create New Recipe</button>
  <ol>
    {{#each recipes as |recipe|}}
    <li>
      {{> recipe-info recipe}}
      <a href="/dashboard/edit/{{recipe.id}}" class="edit-link">Edit recipe</a>
    </li>
    {{/each}}
  </ol>
</section>


<script src="/javascript/add-recipe.js"></script>